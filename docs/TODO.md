# TODO — Improvement Backlog (Wave 7+)

Prioritized backlog from the 2026-07-03 full-codebase review. Items are grouped into six buckets ordered by leverage on user outcomes. Bucket 1 (statistical correctness) takes precedence — a polished agent that misreports a significance call is worse than a rougher agent that calls it correctly. Bucket 2 (backend unification) is the structural fix that retires most of Bucket 1's divergence bugs permanently.

The previous backlog (items 1–35, waves 1–6, all shipped) is archived at [TODO-waves-1-6.md](TODO-waves-1-6.md). Numbering here continues at 36 so `git log --grep "TODO.md #N"` remains unambiguous.

Items 74–116 below were added in the 2026-07-18 follow-up review (agent layer, statistical capabilities, product UX) and are grouped into buckets 7–9; the same numbering and effort conventions apply.

Effort tags: **(S)** ≤ ½ day, **(M)** 1–3 days, **(L)** > 3 days.

---

## Bucket 1 — Statistical correctness

Highest leverage. These items affect what the agent tells users about their experiments.

36. [ ] **Fix effect-size / confidence-interval scale mismatch for GLM families** — for binary/count/heavy-tail metrics, `effect_size` is the raw mean/proportion difference (`model_families.py:230`) but `confidence_interval` is the GLM term CI in log-odds/log-rate units (`model_families.py:473`). Charts draw log-odds error bars around a raw difference (`charts_core.py:59-85`). Report the CI on the same scale as the effect (marginal effect / rate difference), or carry both scales explicitly on `ABTestResult`. _Files:_ `src/statistics/model_families.py`, `src/statistics/models.py`, `src/statistics/charts_core.py`. **(M)**
37. [ ] **Align proportion-test method across backends** — pandas uses `method="score"` (`statsmodels_engine.py:480`), Spark uses `method="wald"` (`pyspark_analyzer.py:695`); same data yields different p-values. Standardize on score and port the pandas guardrails (small-n, expected-cell-count, degenerate inputs) into the Spark path. _Files:_ `src/statistics/pyspark_analyzer.py`, `src/statistics/statsmodels_engine.py`, `tests/test_parity_pandas_spark.py`. **(M)**
38. [ ] **Spark: honor `sequential_config` and multiple-testing correction, or fail loudly** — Spark accepts `sequential_config` and silently ignores it (`pyspark_analyzer.py:979`, `:1186`) and skips BH/FDR in segmented analysis. Apply both, and raise a structured error for any remaining unsupported option instead of returning defaulted fields. _Files:_ `src/statistics/pyspark_analyzer.py`. **(M)**
39. [ ] **Replace observed-power adequacy with MDE-based adequacy** — `is_sample_adequate` is computed from post-hoc power on the realized effect (`analyzer.py:466-473`, `:720`; `pyspark_analyzer.py:1046-1048`), the classic observed-power fallacy. Key adequacy off `achieved_mde` (already computed at `analyzer.py:766`) vs. a target effect instead. _Files:_ `src/statistics/analyzer.py`, `src/statistics/pyspark_analyzer.py`, `src/agent_reporting.py`. **(S)**
40. [ ] **Allocation-ratio-aware SRM** — the SRM check hardcodes an expected 50/50 split (`diagnostics.py:94`, `statsmodels_engine.py:266`, `pyspark_analyzer.py:836`), so an intentional 90/10 holdout trips SRM and silently forces `is_significant=False` (`analyzer.py:524-529`). Add `expected_treatment_ratio` to config / `run_ab_test` and thread it through. _Files:_ `src/statistics/diagnostics.py`, `src/statistics/statsmodels_engine.py`, `src/statistics/pyspark_analyzer.py`, `src/config.py`. **(M)**
41. [x] **Close the deterministic-difference significance leak** — the zero-variance branch returns `p_value=0.0` with `blocks_significance=False` regardless of `small_n` (`model_families.py:278-311`), so two constant-but-unequal points per arm read as significant. Honor `small_n` like every other branch. _Files:_ `src/statistics/model_families.py`. **(S)**
42. [ ] **Fix `total_effect` double-counting** — the combined effect adds the t-test total effect to the proportion effect (`analyzer.py:566-576`; `pyspark_analyzer.py:1077-1085`), overstating impact whenever both tests are significant (the mean difference already includes value from incremental converters). Fix the arithmetic or document the narrow metric construction where addition is valid. _Files:_ `src/statistics/analyzer.py`, `src/statistics/pyspark_analyzer.py`, `src/statistics/summary_builder.py`. **(M)**
43. [ ] **Surface silent statistical fallbacks** — model-fit exceptions silently fall back OLS→CompareMeans (`model_families.py:419-445`), proportion-test exceptions resolve to `p=1.0` (`statsmodels_engine.py:509`; `pyspark_analyzer.py:701`), and power/MDE/required-N exceptions return `0.0`/sentinels (`power_analysis.py:56,87,114`). A systematically failing model currently looks like a clean null. Propagate structured warnings into the rendered report. _Files:_ `src/statistics/model_families.py`, `src/statistics/power_analysis.py`, `src/agent_reporting.py`. **(M)**
44. [ ] **Numeric-precision cleanups** — use `scipy` survival functions instead of `1 - cdf` for tail p-values (`pyspark_analyzer.py:660`, `:711`); estimate NegativeBinomial dispersion instead of hardcoding `alpha=1.0` (`model_families.py:373`); unify the required-N sentinel (`int(1e9)` at `power_analysis.py:100` vs `999999` at `pyspark_analyzer.py:739`). _Files:_ `src/statistics/pyspark_analyzer.py`, `src/statistics/model_families.py`, `src/statistics/power_analysis.py`. **(S)**

---

## Bucket 2 — Backend unification

The structural fix. Most Bucket 1 divergences exist because the two backends re-implement the same statistics independently.

45. [ ] **Extract a shared statistical core** — Spark hand-rolls Cohen's d, power, required-N, Bayesian Monte Carlo, and the proportion test (`pyspark_analyzer.py:621-800`), duplicating `power_analysis.py` / `bayesian.py` / `statsmodels_engine.py`. The Spark path already collects small aggregates to the driver (`pyspark_analyzer.py:1003`), so both backends can call the same backend-agnostic helpers taking `(mean, var, n)` or arrays. Deletes roughly `pyspark_analyzer.py:620-1180` and makes answer divergence structurally impossible. Land after #37/#38 so parity tests pin behavior first. _Files:_ `src/statistics/pyspark_analyzer.py`, `src/statistics/analyzer.py`, `src/statistics/power_analysis.py`, `src/statistics/bayesian.py`, `tests/test_parity_pandas_spark.py`. **(L)**
46. [ ] **Restructure `ABTestResult` and consolidate serialization** — the ~90-field flat dataclass (`models.py:70-182`) forces enormous keyword assembly at three call sites, and the hand-written 80-key `to_legacy_dict` (`models.py:184-273`) plus `canonical_result_as_dict` plus `SparkABTestResult.to_serializable_dict` (`pyspark_analyzer.py:127`) define the alias vocabulary three times. Group fields into nested sub-models (frequentist / proportion / bayesian / sequential / diagnostics) and generate legacy aliases from `_CANONICAL_FIELD_ALIASES`. _Files:_ `src/statistics/models.py`, `src/statistics/analyzer.py`, `src/statistics/pyspark_analyzer.py`. **(L)**
47. [ ] **Centralize numeric configuration** — Bayesian decision thresholds are hardcoded at 0.95/0.05 and ignore `significance_level` (`analyzer.py:588-589`; `charts_extended.py:36-38`); charts hardcode `p<0.05` and 80% power labels (`charts_core.py:214,242,292`; `charts_dashboard.py:243`); magic thresholds for heavy-tail detection, overdispersion, variance ratio, outlier sensitivity, and the n>30 normal cutoff are scattered literals (`model_families.py:77-86,367`; `diagnostics.py:315`; `bayesian.py:81`). Name them as constants on `Config`/engine and thread alpha/power everywhere they render. _Files:_ `src/config.py`, `src/statistics/*`. **(M)**

---

## Bucket 3 — Security & operations

48. [x] **Confine `load_csv` file paths** — the LLM-controlled `filepath` flows unvalidated to `pd.read_csv` (`tooling/loading.py:25` → `data_manager.py:41`), which accepts local paths *and* URLs. "Load the CSV at /app/.env" discloses secrets into chat; internal URLs are SSRF. Resolve against an allowlisted upload/data root, reject non-file schemes and path escapes. **Do this first.** _Files:_ `src/tooling/loading.py`, `src/statistics/data_manager.py`, `app.py`. **(S)**
49. [ ] **Harden deployment defaults** — auth activates only when both `STATAGENT_AUTH_*` vars are set (`auth.py:18-20`), the container binds `0.0.0.0` with no TLS guidance, and `prompt_playground = true` (`.chainlit/config.toml:9`). Add a `STATAGENT_REQUIRE_AUTH` fail-fast flag, disable the playground, and document `CHAINLIT_AUTH_SECRET` as **required** when auth is on (unset, every restart invalidates all sessions and replicas can't share them). _Files:_ `src/auth.py`, `app.py`, `.chainlit/config.toml`, `docs/deployment.md`. **(M)**
50. [ ] **Treat `mode=ro` as the primary SQL guardrail** — the regex denylist in `sql_query_service.py:94-148` is bypassable in principle and over-blocking; the read-only SQLite connection (`query_store.py:219`) is what actually prevents writes. Document that, replace the regex with a real parser (`sqlparse`) or drop it, and fix the naive ` LIMIT n` string-append which can change semantics on compound/UNION queries (`sql_query_service.py:149-150`). _Files:_ `src/sql_query_service.py`, `src/query_store.py`. **(S)**
51. [ ] **Periodic query-store GC + retention policy** — GC runs once per process (`query_store_gc.py:74-88`), so long-lived containers accumulate plaintext session SQLite files (raw uploaded data + full chat history, `query_store.py:83-97,142-151`) indefinitely on the mounted volume. Re-run GC on `on_chat_start` or a timer, and document data-at-rest retention (consider encryption or shorter retention for PII-bearing CSVs). _Files:_ `src/query_store_gc.py`, `src/agent_session.py`, `docs/deployment.md`. **(S)**
52. [ ] **Stop swallowing audit/persistence/config errors** — `_record_audit`, `save_chat_message`, `load_chat_messages`, and `clear_chat_messages` all `except sqlite3.Error: pass` (`query_store.py:95-140`), so a failing audit table produces zero signal; config coercion silently falls back to defaults on bad env values (`config.py:69-84`) and `validate()` failure is non-fatal (`app.py:40-41`). Log warnings on every one of these paths; fail fast on invalid config. _Files:_ `src/query_store.py`, `src/config.py`, `app.py`. **(S)**
53. [ ] **Per-session log correlation** — the JSON formatter drops `extra` fields and no session/request ID is threaded through logs (`observability.py:79-91`), so concurrent sessions can't be triaged. Add a session-scoped correlation ID to all log records and include `extra` in the JSON formatter. _Files:_ `src/observability.py`, `app.py`, `src/agent.py`. **(S)**

---

## Bucket 4 — Architecture & internal API

54. [ ] **Define a real public agent interface for tools** — `AgentProtocol` codifies underscore-private members as the tool contract (`tooling/common.py:14-36`) and tools read/write `agent._last_results` / `agent._last_summary` directly (`tooling/visualization.py:32-57`), while `ABTestingAgent` re-exposes runtime/session internals through ~130 lines of pass-through properties (`agent.py:128-208`). Promote the needed operations to public methods on the session/runtime, point tools at them, and delete the pass-throughs. Includes the concrete bug where `answer_data_question` bypasses the guarded session wrapper (`tooling/analysis.py:310` vs `agent_session.py:94-98`) and dies with a raw `AttributeError` when the service is unconfigured. _Files:_ `src/tooling/common.py`, `src/tooling/*.py`, `src/agent.py`, `src/agent_session.py`. **(M)**
55. [ ] **Typed diagnostics accessors** — the nested `result.diagnostics["experiment_quality"][...]` walk is hand-rolled in three modules (`summary_builder.py:55-96,315-376`; `agent_reporting.py:394-440,466-476`); one key rename breaks summaries and reports differently. Add typed accessors (or a small dataclass) next to where the payload is built. _Files:_ `src/statistics/diagnostics.py`, `src/statistics/summary_builder.py`, `src/agent_reporting.py`. **(S)**
56. [ ] **Consolidate the chart mixins** — `charts_core` / `charts_dashboard` / `charts_extended` is a size-based split, not an API seam; all three depend on `ABTestVisualizer` internals (`charts_core.py:28`, `visualizer.py:24,59`). Collapse into one `charts.py` (keep `chart_builders` + `chart_catalog`), and thread `significance_level`/`power_threshold` into chart labels (see #47). _Files:_ `src/statistics/charts_*.py`, `src/statistics/visualizer.py`. **(M)**
57. [ ] **Dedupe the Best Guess Mode renderers** — `render_load_and_auto_analyze_report` (`agent_reporting.py:291-330`) and `render_auto_configure_and_analyze_report` (`:357-376`) copy-paste the config table + warnings + results block; extract the shared body. Also unify the error contract: half the tools return uncoded free-text ("No data loaded...") while others raise coded errors, and classification string-matches on message prose (`agent_reporting.py:69-80`). _Files:_ `src/agent_reporting.py`, `src/tooling/*.py`. **(S)**
58. [ ] **Remove dead surface** — `ABTestingAgent.arun` has zero call sites (`agent.py:318-324`); the interactive REPL block (`agent.py:340-352`) is vestigial for a Chainlit app; `truncate_iterable_for_llm` and `AgentSessionState.clear_analysis_state` are test-only; analyzers call `summary_builder._generate_recommendations` through "backward-compatible" shims nothing uses (`analyzer.py:886-888`, `pyspark_analyzer.py:1244-1246`). Delete. _Files:_ `src/agent.py`, `src/agent_session.py`, `src/output_truncation.py`, `src/statistics/analyzer.py`, `src/statistics/pyspark_analyzer.py`. **(S)**
59. [ ] **Update `docs/architecture.md`** — it documents only the agent→analyzer→reporting spine and omits `agent_session`, the `tooling/` registry, `query_store*`, `sql_query_service`, and `Config` — the layers where the coupling above actually lives. Rewrite after #54 lands so it documents the new contract. _Files:_ `docs/architecture.md`. **(S)**
60. [ ] **Deduplicate session bootstrap** — the query-store path is built independently in `app.py:64-75` and `agent_session.py:54-58`; `run_startup_gc` runs at import *and* per-session (`app.py:43`, `agent_session.py:59`); `load_dotenv()` runs twice (`app.py:24`, `agent.py:44`). Single-source the storage layout and startup side effects. _Files:_ `app.py`, `src/agent_session.py`, `src/agent.py`. **(S)**

---

## Bucket 5 — Tests, CI, packaging

61. [x] **Enforce mypy in CI** — `mypy src` runs with `continue-on-error: true` (`.github/workflows/ci.yml:39-42`), so type errors never fail the build. Remove the escape hatch, extend to `app.py` (and `tests/` as a stretch), then progressively tighten `ignore_missing_imports` / `follow_imports="silent"` (`pyproject.toml:57-63`). _Files:_ `.github/workflows/ci.yml`, `pyproject.toml`. **(S)**
62. [ ] **Make the Spark CI job actually test Spark** — the spark job runs only two files (`ci.yml:98`); `test_pyspark_property_setters.py` and `test_analyzer_protocol.py` never execute with real Spark in any job, and `_create_spark_or_skip` converts bootstrap failures into skips (`test_parity_pandas_spark.py:50-54`), so the job can go green having executed nothing. Run all Spark-marked tests in the spark job and make Spark bootstrap failure a hard failure there (env flag, e.g. `STATAGENT_REQUIRE_SPARK=1`). _Files:_ `.github/workflows/ci.yml`, `tests/test_parity_pandas_spark.py`, `tests/test_pyspark_analyzer.py`. **(S)**
63. [ ] **Add coverage measurement with a threshold** — no `pytest-cov` anywhere; 6.5k lines of tests with zero signal on what the mocked agent tests exercise. Add `pytest-cov` to the dev extra, `--cov=src --cov-fail-under=<current baseline>` in CI, and ratchet upward. _Files:_ `pyproject.toml`, `.github/workflows/ci.yml`. **(S)**
64. [x] **Build CI from the lockfile** — CI runs `pip install ".[dev]"` (`ci.yml:29`), resolving fresh every run, while the Dockerfile uses `uv sync --frozen`; CI and the shipped image can test different dependency versions. Switch CI to `uv sync --frozen --extra dev`. _Files:_ `.github/workflows/ci.yml`. **(S)**
65. [x] **Make the Python version claim honest** — `requires-python = ">=3.9"` (`pyproject.toml:10`) but CI tests only 3.11 and mypy pins 3.11; langchain/langgraph 1.0 floors likely exclude 3.9 anyway. Either add a 3.10–3.12 matrix or raise the floor to match reality. _Files:_ `pyproject.toml`, `.github/workflows/ci.yml`. **(S)**
66. [ ] **Rename the package from `src`** — the wheel installs a top-level package literally named `src` (`pyproject.toml:37-39`, `top_level.txt`), colliding with any other src-layout package in the same environment, and `src/__init__.py` eagerly imports LangChain. Move to `src/statistical_test_agent/` (or rename the dir), update all `from src....` imports, and make `__init__` lazy. Mechanical but touches every file — land between waves, not during one. _Files:_ everything. **(M)**
67. [ ] **Fold PR-history tests into feature suites** — `tests/test_pr_review_fixes.py` organizes tests by PR-comment number and asserts rendered substrings (`"(n=3)"` at `:59`, `"ratio=2"` at `:122-125`); multiple suites bind unbound private methods onto `SimpleNamespace` stubs (`test_pyspark_diagnostics.py:16`, `test_pyspark_seed.py:17`) or drive privates directly (`test_analyzer_comprehensive.py:802-836`). Migrate to behavior-level assertions in feature-named files; prerequisite for the Bucket 2 refactors. _Files:_ `tests/`. **(M)**
68. [ ] **Fix the vacuous golden-task skip** — `tests/eval/test_prompt_coverage.py:33-37` skips any golden task whose expected tool isn't in a hand-maintained `KNOWN_AGENT_TOOLS` set, so a renamed/unregistered tool passes vacuously. Derive the set from the real tool registry and fail on unknown tools. Also drop the redundant second invocation in CI (`ci.yml:66`). _Files:_ `tests/eval/test_prompt_coverage.py`, `.github/workflows/ci.yml`. **(S)**

---

## Bucket 6 — Hygiene quick wins

69. [x] **Untrack `.planning/`** — internal planning scratch files are committed (`git ls-files .planning/`) yet excluded from Docker and lint. Add to `.gitignore` and `git rm -r --cached`. _Files:_ `.gitignore`. **(S)**
70. [ ] **Ship a production image without dev tooling** — both `uv sync` calls use `--extra dev` (`Dockerfile:30,40`) and stage 2 copies all of `/app` (`Dockerfile:59`), so pytest/ruff/mypy and `scripts/` land in the runtime image. Sync without `dev` for the runtime venv and copy only `.venv` + needed source. _Files:_ `Dockerfile`. **(S)**
71. [x] **Lint `scripts/` in CI** — `ruff check src tests app.py` (`ci.yml:37`) skips `scripts/`, which currently carries 9 ruff findings (F541/I001/F401). Lint the whole repo: `ruff check .`. _Files:_ `.github/workflows/ci.yml`, `scripts/`. **(S)**
72. [ ] **Bundle the loading spinner locally** — `public/custom.js:8` hot-links a Wikimedia GIF, an external dependency fetched every session that breaks air-gapped/CSP-locked deployments. Ship the asset in `public/`. _Files:_ `public/custom.js`, `public/`. **(S)**
73. [ ] **Clean repo-root screenshots** — seven wave-era PNGs sit at the repo root (gitignored but noisy); move keepers to `docs/img/` and delete the rest. Note the blanket `*.png` gitignore also silently ignores any *intended* doc screenshot — scope it. _Files:_ `.gitignore`, `docs/`. **(S)**

---

## Bucket 7 — Agent & prompt fidelity

From the 2026-07-18 review of the LLM/agent layer. Highest leverage here: rules that keep the model from miscalling or misstating statistics, and guards against runaway token burn.

74. [ ] **Statistical-fidelity guardrails in the system prompt** — `system.md:4-6` mandates verbatim table echo but has zero rules about numeric claims: nothing stops the model from stating p-values without a tool result, paraphrasing "not significant" as "no effect", or dropping CIs when summarizing. Add explicit rules: never cite a statistic absent from a tool result, always pair significance claims with p-value + CI, never infer causation, and surface guardrail triggers (SRM, low power) whenever they appear in tool output. _Files:_ `src/prompts/system.md`, `src/prompts/__init__.py` (bump `PROMPT_VERSION`), `tests/eval/test_prompt_coverage.py`. **(S)**
75. [ ] **Cap the ReAct loop and stop tool-failure retry spirals** — the graph is invoked with no config (`agent.py:282,294`), so no `recursion_limit` applies; tool errors are returned as ordinary strings (`agent_reporting.py:100`) that look like successful observations, inviting infinite retry of a deterministically failing tool (e.g. `GROUP_LABELS_NOT_SET`). Add a `recursion_limit` and a prompt rule: on `error_code=` results, explain and ask the user instead of retrying the identical call. _Files:_ `src/agent.py`, `src/prompts/system.md`, `src/config.py`. **(S)**
76. [ ] **Bound the conversation context window** — the entire `chat_history` is re-sent on every turn (`agent.py:292-294`) and resume rehydrates every persisted message (`agent.py:114-126`), with no trimming or summarization anywhere. Long sessions grow cost linearly and eventually blow the context window. Apply `trim_messages` (or a sliding window keeping the last analysis result) before invoke. _Files:_ `src/agent.py`, `src/agent_session.py`, `src/config.py`. **(S/M)**
77. [ ] **Clarification branch for ambiguous auto-configuration** — the prompt hard-codes "NO QUESTIONS ASKED" for best-guess mode (`system.md:14`), yet `detect_columns` returns candidate lists (`analyzer.py:134`) and `auto_configure` silently picks one; with multiple plausible group columns or a >2-value group column, a wrong guess runs a full confidently-wrong analysis. Add an ambiguity branch: present the detected mapping and ask for confirmation when detection is ambiguous. _Files:_ `src/prompts/system.md`, `src/tooling/loading.py`, `src/statistics/data_manager.py`, `tests/eval/golden_tasks.py`. **(M)**
78. [ ] **Replace comma-split parsing in `set_group_labels`** — `loading.py:237` parses LLM input via `x.split(",")` unpacking, which crashes on labels containing commas or a single emitted label; neighboring multi-arg tools already use `StructuredTool` (`loading.py:230,240`). Convert to a two-arg `StructuredTool`. _Files:_ `src/tooling/loading.py`. **(S)**
79. [ ] **Give `plan_sample_size` / `compute_ratio_metric` real schemas** — both take a raw JSON string parsed by hand (`analysis.py:46,152`), so the LLM sees no parameter schema and malformed calls surface only at runtime. Converting to `StructuredTool` with typed args puts the schema in the prompt for free and deletes the manual validation. _Files:_ `src/tooling/analysis.py`. **(S)**
80. [ ] **LLM timeout/retry/transient-failure configurability** — `ChatOpenAI` is constructed with only model + temperature (`agent.py:81-85`): no `request_timeout`, no `max_retries`; a single 429/500/timeout becomes generic `AGENT_EXECUTION_FAILED` (`agent.py:309-316`). Add config knobs and map rate-limit/timeout errors to distinct user-facing codes. _Files:_ `src/agent.py`, `src/config.py`, `src/agent_reporting.py`. **(S)**
81. [ ] **Truncate the full-analysis renderer** — row-based truncation (#26) covers only dataframe renderers (`output_truncation.py:16-28`); `render_full_analysis_output` loops over all segments untruncated (`agent_reporting.py:544-551`) — a 500-segment file dumps a giant table into chat *and* future LLM turns. Cap segment rows with a "N more segments" suffix. _Files:_ `src/agent_reporting.py`, `src/output_truncation.py`. **(S)**
82. [ ] **Harden against indirect prompt injection via data** — raw CSV column names and values flow unescaped into model context (`agent_reporting.py:675+`, `:578-584`), and `system.md` has no untrusted-data rule; a CSV containing "Ignore previous instructions…" in a string column is a live injection vector. Delimit data blocks in tool outputs and add an explicit instruction-hierarchy rule to the prompt. _Files:_ `src/prompts/system.md`, `src/agent_reporting.py`. **(M)**
83. [ ] **Expand golden-task coverage to unrouted and adversarial behaviors** — `tests/eval/golden_tasks.py:27-72` covers only 6 happy-path routings; nothing evaluates ratio-metric or `query_data` routing, follow-up turns, analyze-before-load, ambiguous-input clarification, or error recovery. The live eval asserts only tool names (`test_live_tools.py:49-52`) — never that numbers in the final answer appear in tool output (no hallucination check). Distinct from #68, which fixes the skip mechanism. _Files:_ `tests/eval/golden_tasks.py`, `tests/eval/test_live_tools.py`. **(M)**
84. [ ] **Externalize and version the SQL-planner prompt; add planner evals** — the main system prompt was externalized/versioned (#23), but `OpenAISQLPlanner` carries a second, inline, unversioned system prompt (`sql_query_service.py:48-58`) with few-shot examples and no eval fixtures. Move it under `src/prompts/`, version it, and add golden SQL-plan fixtures. _Files:_ `src/sql_query_service.py`, `src/prompts/`, `tests/eval/`. **(S)**
85. [ ] **Persist token usage per session and estimate cost** — `TokenUsageCallback` tallies per run and only logs (`agent.py:298-307`); nothing is persisted, no cumulative per-session totals, no cost estimate. Persist a usage row per run into `query_store` so spend per session is auditable. Distinct from #53 (log correlation). _Files:_ `src/observability.py`, `src/query_store.py`, `src/agent.py`. **(S/M)**
86. [ ] **Stream responses instead of send-then-replace** — the UI sends an empty message and wholesale-updates it after a blocking `agent.invoke` (`app.py:153-158`; `agent.py:294`); no `astream`/`ainvoke` anywhere in the agent layer. Long full-analysis runs sit silent for tens of seconds. Wire `astream_events` through `agent.run`. _Files:_ `src/agent.py`, `app.py`. **(M)**
87. [ ] **Actually land per-tool latency metrics** — archived #20 specified per-tool timing, but shipped code only logs start/complete events with no duration (`tooling/analysis.py:261` vs `:271`); no timing wrapper exists in `tooling/common.py`. Add a timing decorator and log `duration_ms` per tool call. _Files:_ `src/tooling/common.py`, `src/tooling/*.py`. **(S)**

---

## Bucket 8 — Statistical capabilities & correctness

From the 2026-07-18 review of the statistics layer. Items 88–94 are correctness issues; 95–102 are missing capabilities.

88. [ ] **Remove the AA-failure "bootstrap rebalancing" p-hack** — when the pre-period balance check fails, `bootstrap_balanced_control` resamples control rows up to 1000 times and keeps the subsample with the *highest* AA p-value (`experiment_design.py:92-119`, wired at `segment_preparer.py:199-210`). Selecting data conditional on a test outcome biases every downstream p-value and silently discards control rows. Replace with adjustment (CUPED/DiD already exist) or downgrade to a loud warning. _Files:_ `src/statistics/experiment_design.py`, `src/statistics/segment_preparer.py`, `src/statistics/statsmodels_engine.py`. **(M)**
89. [ ] **Stop CUPED-adjusted values leaking into proportion/Bayesian paths** — CUPED overwrites `treatment_post_aligned`/`control_post_aligned` in place (`analyzer.py:429-431`), and those residuals then feed `run_proportion_test` (`analyzer.py:512-515`), which counts `!= 0` as "conversions" (`statsmodels_engine.py:403`): for a binary metric the adjusted values are continuous, so both arms report conversion ≈ 1.0. The Bayesian interval and `total_effect` also silently switch scale. Restrict adjusted arrays to the primary effect estimation, or gate CUPED to continuous metrics. _Files:_ `src/statistics/analyzer.py`, `src/statistics/covariate_resolver.py`, `src/statistics/statsmodels_engine.py`. **(M)**
90. [ ] **Segmented analysis never computes the Overall result** — `run_segmented_analysis` returns per-segment results only (`analyzer.py:795-826`; `pyspark_analyzer.py:1201-1227`), so `run_full_analysis` never reports the headline overall effect for segmented data — users see only slice-level calls plus a BH correction. Include an Overall run alongside segments (and decide whether it joins the FDR family). _Files:_ `src/statistics/analyzer.py`, `src/statistics/pyspark_analyzer.py`, `src/statistics/summary_builder.py`. **(M)**
91. [ ] **Add a treatment×segment heterogeneity (interaction) test** — the segmented path runs independent per-segment tests and BH-corrects them (`analyzer.py:843-889`) but never tests whether the effect *differs* across segments; "A significant, B not" is presented as heterogeneity with no interaction p-value to support it. Add a segment×treatment interaction term (the GLM scaffolding at `model_families.py:344-418` already supports extra regressors) and report it next to the per-segment table. _Files:_ `src/statistics/analyzer.py`, `src/statistics/model_families.py`, `src/statistics/models.py`, `src/agent_reporting.py`. **(M)**
92. [ ] **Render FDR-adjusted significance in segment tables** — `_apply_multiple_testing_correction` populates `p_value_adjusted`/`is_significant_adjusted` (`analyzer.py:872-889`), yet the reporting layer renders raw `p_value` and stars from unadjusted `is_significant` (`agent_reporting.py:225-238`, `:545-549`); the adjusted call survives only as an aggregate count. Render adjusted p/significance (or both columns) wherever per-segment significance is displayed. _Files:_ `src/agent_reporting.py`, `src/statistics/summary_builder.py`. **(S)**
93. [ ] **Give the DiD estimate its inference** — `estimate_did_effect` fits an HC3 OLS on the changes but returns only `params[1]`, discarding the SE/p-value/CI it just computed (`model_families.py:513-524`); `did_effect` is rendered (`agent_reporting.py:214`) and aggregated (`summary_builder.py:116-117`) as if validated while significance comes from the post-only comparison (`analyzer.py:491`). Return DiD SE/p/CI and surface them. _Files:_ `src/statistics/model_families.py`, `src/statistics/models.py`, `src/agent_reporting.py`. **(S)**
94. [ ] **Base sequential alpha-spending on observed information, not look counters** — `information_fraction = look_index / max_looks` (`statsmodels_engine.py:118`) assumes equally-spaced looks; peeking at 10% of planned sample with `look_index=2, max_looks=5` spends alpha as if 40% of information were in. Accept actual `n_current`/`n_planned` (or explicit fraction) in `SequentialConfig`. _Files:_ `src/statistics/sequential_config.py`, `src/statistics/statsmodels_engine.py`, `src/statistics/analyzer.py`. **(M)**
95. [ ] **Family-aware Bayesian models** — `run_bayesian_test` samples normal/t posteriors around raw means regardless of metric type (`bayesian.py:77-93`), even when the frequentist side chose GLM Binomial/Poisson for the same data; credible intervals for conversion rates can extend below 0/above 1. Add Beta-Binomial / Gamma-Poisson conjugate models keyed off the inferred `metric_type`. _Files:_ `src/statistics/bayesian.py`, `src/statistics/analyzer.py`, `src/statistics/pyspark_analyzer.py`. **(M)**
96. [ ] **Add a nonparametric/bootstrap inference path** — normality/Levene diagnostics are computed and surfaced (`diagnostics.py:173-217`) but nothing consumes them; there is no Mann-Whitney/permutation/bootstrap-CI option anywhere, and the heavy-tail branch offers only Huber RLM or log-transform (`model_families.py:386-411`). Add a permutation test or bootstrap CI, auto-suggested when normality fails at moderate n. _Files:_ `src/statistics/model_families.py`, `src/statistics/statsmodels_engine.py`, `src/statistics/diagnostics.py`. **(M)**
97. [ ] **Balance/leakage guardrail on user-supplied covariates** — `resolve_covariate_columns` accepts any numeric column (`covariate_resolver.py:151-180`) with no check that it is pre-treatment or balanced across arms; adjusting for a post-treatment/leaky covariate silently biases the treatment coefficient. Add a per-covariate across-arm balance test with a warning (or refusal). _Files:_ `src/statistics/covariate_resolver.py`, `src/statistics/diagnostics.py`, `src/statistics/models.py`. **(S)**
98. [ ] **Cluster-robust inference for repeated experimental units** — duplicate/cross-arm unit detection warns that independence is violated (`diagnostics.py:60-89`) but inference proceeds with row-level OLS-HC3 and Welch tests regardless. Offer cluster-robust SEs grouped by the unit column (`cov_type="cluster"`) or automatic pre-aggregation to one row per unit. _Files:_ `src/statistics/model_families.py`, `src/statistics/diagnostics.py`, `src/statistics/analyzer.py`. **(M)**
99. [ ] **Bring ratio metrics into the full pipeline** — `compute_ratio_metric` hard-requires pandas (`tooling/analysis.py:62`), so Spark datasets have no ratio path; the tool is also excluded from segmented runs and BH correction, so ratio results never get multiplicity control. And `_arm_ratio_and_variance` silently truncates to the shorter array assuming row pairing (`ratio_metric.py:52-57`) — enforce or validate pairing inside the function. _Files:_ `src/tooling/analysis.py`, `src/statistics/ratio_metric.py`, `src/statistics/pyspark_analyzer.py`. **(M)**
100. [ ] **Duration/minimum-runtime guidance in sample-size planning** — `plan_sample_size` returns per-arm counts (`tooling/analysis.py:236-245`) but never converts to days given expected daily traffic, and nothing warns about sub-week runtimes or partial business cycles. Add optional `daily_traffic_per_arm` → estimated runtime in days, plus a minimum-runtime warning (< 7 days). _Files:_ `src/tooling/analysis.py`, `src/statistics/power_analysis.py`. **(S)**
101. [ ] **Effect-size reporting gaps for binary metrics** — results carry only Cohen's d even for proportion metrics (Cohen's h is the standard; the planner already uses h at `tooling/analysis.py:200`); `achieved_mde` is always solved with `TTestIndPower` regardless of family (`analyzer.py:775-780`); and no relative lift with CI exists on the frequentist side (only Bayesian, `models.py:168`). Add h for binary results, family-aware MDE, and a relative-effect field with CI. _Files:_ `src/statistics/power_analysis.py`, `src/statistics/analyzer.py`, `src/statistics/models.py`. **(M)**
102. [ ] **Add an always-valid (mSPRT) sequential option** — the sequential layer only supports group-sequential designs with pre-declared `max_looks` (`sequential_config.py:99-147`); users who peek arbitrarily often (the common product pattern) have no valid option and will misuse the fixed-look machinery. Add mixture-SPRT p-values / confidence sequences as a third spending mode. _Files:_ `src/statistics/sequential_config.py`, `src/statistics/statsmodels_engine.py`. **(L)**

---

## Bucket 9 — Product UX & delivery

From the 2026-07-18 review of the app/UX/ops surface.

103. [ ] **Real welcome screen + native starter prompts with one-click sample data** — `chainlit.md:1-10` is still the untouched "Welcome to Chainlit!" boilerplate; starters are DOM-scraped in by a `MutationObserver` (`public/custom.js:372-393`, fragile selectors at `:20`); and the bundled `data/sample_ab_data.csv` is allowlisted (`data_paths.py:42`) but undiscoverable. Write a real welcome screen and register starters via `@cl.set_starters`, including "Analyze the sample dataset". _Files:_ `chainlit.md`, `app.py`, `public/custom.js`, `data/`. **(S)**
104. [ ] **Restore full analysis state on chat resume, not just chat text** — `on_chat_resume` (`app.py:234-242`; `agent.py:116`) only rehydrates `_chat_history`; the loaded dataframe, results, and charts are gone, so "show me the charts again" after reconnect fails with "No data loaded" (`tooling/visualization.py:26`). Yet `raw_data`, `analysis_segment_results`, and `analysis_summary` are already persisted per session (`query_store.py:142-192`) and never read back. Reload them on resume. _Files:_ `app.py`, `src/agent.py`, `src/agent_session.py`, `src/query_store.py`. **(M)**
105. [ ] **Report export / download** — zero export code exists (no `cl.File`, `to_html`, or download path); results live only as chat text plus ephemeral inline figures (`app.py:97-118`). Add a "Download report" action bundling markdown summary + charts into self-contained HTML, plus `analysis_segment_results` as CSV via `cl.File`, sourced from what the query store already persists. _Files:_ `app.py`, `src/agent_reporting.py`, `src/statistics/visualizer.py`, `src/query_store.py`. **(M)**
106. [ ] **Data preview and validation feedback after upload** — the upload flow (`app.py:129-150`) sends only a text blob; `load_data` already computes `sample` rows and `dtypes` (`data_manager.py:42-47`) but they are never rendered (no `cl.Dataframe` usage anywhere). Render the first N rows plus a per-column missing-value summary after load so users catch wrong delimiters/dtypes before analyzing. _Files:_ `app.py`, `src/agent_reporting.py`, `src/statistics/data_manager.py`. **(S)**
107. [ ] **Broaden ingestion beyond CSV; fix the upload-cap story** — only `.csv` is accepted (`.chainlit/config.toml:17`, `app.py:131`, `data_manager.py:41`), and the 10 MB cap (`config.toml:23`) means larger files must be side-loaded via volume mount (`docs/deployment.md:90-93`) — a path no end user will discover from chat. Add parquet/TSV/Excel readers, and when a file is rejected for size, tell the user the concrete alternative. _Files:_ `.chainlit/config.toml`, `app.py`, `src/statistics/data_manager.py`, `src/data_paths.py`, `src/agent_reporting.py`. **(M)**
108. [ ] **Progressive step feedback for long-running work** — the entire `agent.run` call is one opaque await (`app.py:145-158`); uploads get a static "Processing..." message and everything else an empty bubble; no `cl.Step` usage exists. Wrap load → analyze → chart generation in named `cl.Step`s so multi-minute runs show live progress. _Files:_ `app.py`, `src/agent.py`, `src/tooling/loading.py`. **(M)**
109. [ ] **Fix or remove the fake history sidebar** — `public/custom.js:231-307` builds a conversation list from `localStorage` titles; clicking any non-active entry does literally nothing (`:299-303`), while actual resumable server-side threads (`app.py:234`) are unreachable from it. Wire it to Chainlit's thread/data layer for real session switching (rename/delete/share), or delete the panel. _Files:_ `public/custom.js`, `app.py`. **(M)**
110. [ ] **Humanize number formatting in reports** — effects/means render as raw `{:.4f}` (`agent_reporting.py:242-243`, `:454-464`), p-values as `{:.6f}` with no `p < 0.0001` collapsing, no thousands grouping or currency-aware formatting for revenue metrics. Add a shared display formatter (grouped digits, adaptive precision, `p<0.001` style, optional unit hint from column naming). Distinct from #47 (threshold centralization). _Files:_ `src/agent_reporting.py`, `src/statistics/summary_builder.py`. **(S)**
111. [ ] **Warn before a second CSV silently replaces the loaded dataset** — loading a new file overwrites state without confirmation (`agent_session.py:77-84` → `save_raw_dataframe(..., if_exists="replace")`; `app.py:133`); a stray upload destroys the current analysis context. Detect an active analysis and confirm replacement (or offer a new session). _Files:_ `app.py`, `src/tooling/loading.py`, `src/agent_session.py`. **(S)**
112. [ ] **Accessibility pass on charts and loading UI** — significance is encoded purely as green/red (`visualizer.py:36-38`), failing red-green colorblind users; the spinner ignores `prefers-reduced-motion` (`public/custom.js:309-335`). Use a colorblind-safe palette plus shape/icon encoding for significance, and honor reduced-motion. _Files:_ `src/statistics/visualizer.py`, `src/statistics/charts_*.py`, `public/custom.js`, `public/custom.css`. **(S)**
113. [ ] **Add pre-commit hooks and a task runner** — ruff/mypy config exists (`pyproject.toml:44-63`) but no `.pre-commit-config.yaml` and no `Makefile`/`justfile`; contributors must hand-copy commands from `docs/development.md`. A two-hook pre-commit (ruff, ruff-format) plus a `Makefile` with `test`/`lint`/`run`/`sample-data` targets. _Files:_ `.pre-commit-config.yaml` (new), `Makefile` (new), `docs/development.md`. **(S)**
114. [ ] **Write a CONTRIBUTING guide** — no `CONTRIBUTING.md` exists, yet contributor-facing conventions are scattered: the parity-test mandate for Spark changes (`TODO-waves-1-6.md:82`), `docs/testing.md`, and the mypy burn-down list (`pyproject.toml:65-76`). Consolidate setup, the parity rule, and how to pick up TODO items. _Files:_ `CONTRIBUTING.md` (new), `docs/development.md`. **(S)**
115. [ ] **Ship a Spark-enabled Docker image variant** — `docs/deployment.md:84-93` tells users to hand-craft a custom image with Java + `--extra spark`; the `Dockerfile` has no build-arg/target for it, and CI has no Docker build job at all. Add a `runtime-spark` stage and a CI job that builds both tags. Distinct from #70 (dev-dep removal). _Files:_ `Dockerfile`, `.github/workflows/ci.yml`, `docs/deployment.md`. **(M)**
116. [ ] **Add a container HEALTHCHECK** — the `Dockerfile` exposes 8000 with no `HEALTHCHECK` (`Dockerfile:66-68`), while `docs/deployment.md:94-97` recommends multi-replica deployments behind a load balancer, where orchestrators need a liveness signal. Add a `HEALTHCHECK` hitting the Chainlit HTTP endpoint. _Files:_ `Dockerfile`, `docs/deployment.md`. **(S)**

---

## Suggested sequencing

1. **#48** (file-path confinement) — one small PR, real exposure.
2. **#36** (effect/CI scale mismatch) — most user-visible correctness bug.
3. **#61–#64** (CI enforcement) — cheap, and everything after needs a trustworthy gate.
4. **#37, #38, #41** (backend divergence + significance leak) — pin behavior with parity tests.
5. **#45** (shared statistical core) — the big one; write a plan first, land behind the parity tests from step 4.
6. Everything else opportunistically; #66 (package rename) between waves.

For the 2026-07-18 additions (74–116): the cheap, high-leverage picks are **#74** (prompt fidelity rules) and **#75** (recursion cap) — both small PRs that directly protect answer correctness; **#88** (remove the AA-rebalancing p-hack) and **#89** (CUPED scale leak) are the most serious new correctness bugs; **#92** and **#93** are (S) inference-reporting fixes that pair naturally; and **#103**, **#106**, **#110**, **#111** are quick UX wins.
